apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: thanawy-pdb
  labels:
    app: thanawy
spec:
  # Ensure at least 50% of pods remain available during voluntary disruptions
  minAvailable: 50%
  selector:
    matchLabels:
      app: thanawy
# خطوات الاختبار اليدوي لنظام تسجيل الدخول وإنشاء الحساب

## 🎯 الهدف
اختبار جميع السيناريوهات المذكورة في `LOGIN_SYSTEM_TEST.md` (السطور 37-57)

---

## 📋 قائمة الاختبارات

### ✅ أ. اختبار التحقق من الصحة

#### 1. إدخال بريد إلكتروني غير صحيح
**الخطوات:**
1. افتح المتصفح على `http://localhost:3000/login`
2. أدخل بريد إلكتروني غير صحيح في حقل البريد (مثل: `test` بدون @)
3. اضغط "تسجيل الدخول"

**النتيجة المتوقعة:**
- [ ] يجب أن يظهر خطأ: "البريد الإلكتروني غير صالح"
- [ ] الحقل يجب أن يظهر بحدود حمراء
- [ ] لا يمكن إرسال النموذج

**للتسجيل (Register):**
1. افتح `http://localhost:3000/register`
2. كرر نفس الخطوات

---

#### 2. إدخال كلمة مرور قصيرة (< 8 أحرف)
**الخطوات:**
1. افتح `http://localhost:3000/login`
2. أدخل بريد صحيح: `test@example.com`
3. أدخل كلمة مرور قصيرة: `Test1!` (7 أحرف فقط)
4. اضغط "تسجيل الدخول"

**النتيجة المتوقعة:**
- [ ] يجب أن يظهر خطأ: "كلمة المرور يجب أن تتكون من 8 أحرف على الأقل"
- [ ] الحقل يجب أن يظهر بحدود حمراء
- [ ] لا يمكن إرسال النموذج

**للتسجيل (Register):**
- نفس الاختبار، لكن قد تكون متطلبات كلمة المرور أكثر صرامة (حرف كبير، صغير، رقم، رمز خاص)

---

#### 3. ترك حقل فارغ
**الخطوات:**
1. افتح `http://localhost:3000/login`
2. اترك حقل البريد فارغ
3. اترك حقل كلمة المرور فارغ
4. اضغط "تسجيل الدخول"

**النتيجة المتوقعة:**
- [ ] يجب أن يظهر خطأ: "البريد الإلكتروني مطلوب"
- [ ] يجب أن يظهر خطأ: "كلمة المرور مطلوبة"
- [ ] كلا الحقلين يجب أن يظهرا بحدود حمراء
- [ ] لا يمكن إرسال النموذج

---

### ✅ ب. اختبار تسجيل الدخول الناجح

#### 4. إدخال بيانات صحيحة → تسجيل دخول ناجح
**الخطوات:**
1. تأكد من وجود حساب تجريبي:
   ```bash
   npm run create-test-account
   ```
   أو استخدم:
   - البريد: `test@example.com`
   - كلمة المرور: `Test123!@#`

2. افتح `http://localhost:3000/login`
3. أدخل البريد: `test@example.com`
4. أدخل كلمة المرور: `Test123!@#`
5. اضغط "تسجيل الدخول"

**النتيجة المتوقعة:**
- [ ] يجب أن يتم تسجيل الدخول بنجاح
- [ ] يجب أن تظهر رسالة نجاح: "تم تسجيل الدخول بنجاح!"
- [ ] يجب أن يتم إعادة التوجيه إلى الصفحة الرئيسية (`/`)

---

#### 5. يجب أن يتم حفظ الرمز المميز في localStorage/sessionStorage
**الخطوات:**
1. بعد تسجيل الدخول الناجح (من الاختبار السابق)
2. افتح Developer Tools (F12)
3. اذهب إلى تبويب "Application" (أو "Storage" في بعض المتصفحات)
4. افتح "Local Storage" → `http://localhost:3000`
5. افتح "Session Storage" → `http://localhost:3000`

**النتيجة المتوقعة:**
- [ ] يجب أن تجد `auth_token` في localStorage أو sessionStorage
- [ ] يجب أن تجد `user_data` في localStorage أو sessionStorage
- [ ] Token يجب أن يكون بصيغة JWT (ثلاثة أجزاء مفصولة بنقاط: `xxx.yyy.zzz`)

---

#### 6. يجب أن يتم إعادة التوجيه إلى الصفحة الرئيسية
**النتيجة المتوقعة:**
- [ ] يجب أن تتغير URL من `/login` إلى `/` (الصفحة الرئيسية)
- [ ] يجب أن تظهر الصفحة الرئيسية بدلاً من صفحة تسجيل الدخول
- [ ] يجب أن تظهر معلومات المستخدم (مثل الاسم أو البريد) في الصفحة الرئيسية إذا كانت متاحة

---

### ✅ ج. اختبار معالجة الأخطاء

#### 7. إدخال بيانات خاطئة → رسالة خطأ واضحة
**الخطوات:**
1. افتح `http://localhost:3000/login`
2. أدخل بريد صحيح: `test@example.com`
3. أدخل كلمة مرور خاطئة: `WrongPassword123!`
4. اضغط "تسجيل الدخول"

**النتيجة المتوقعة:**
- [ ] يجب أن يظهر خطأ: "بيانات تسجيل الدخول غير صحيحة"
- [ ] يجب أن تظهر رسالة خطأ واضحة باللغة العربية
- [ ] يجب أن يهتز النموذج (shake animation)
- [ ] يجب أن يظهر خطأ في حقل كلمة المرور

---

#### 8. محاولة تسجيل دخول متكررة → CAPTCHA بعد 3 محاولات
**الخطوات:**
1. افتح `http://localhost:3000/login`
2. حاول تسجيل الدخول ببيانات خاطئة 3 مرات متتالية:
   - المحاولة 1: `wrong@example.com` / `WrongPassword1!`
   - المحاولة 2: `wrong@example.com` / `WrongPassword2!`
   - المحاولة 3: `wrong@example.com` / `WrongPassword3!`
3. في المحاولة الرابعة

**النتيجة المتوقعة:**
- [ ] بعد المحاولة الثالثة، يجب أن يظهر CAPTCHA
- [ ] يجب أن تظهر رسالة: "يرجى إكمال التحقق من CAPTCHA للمتابعة"
- [ ] يجب أن يظهر widget CAPTCHA (hCaptcha أو reCAPTCHA)
- [ ] لا يمكن إرسال النموذج بدون إكمال CAPTCHA

---

#### 9. محاولة تسجيل دخول بعد rate limit → رسالة انتظار
**الخطوات:**
1. افتح `http://localhost:3000/login`
2. حاول تسجيل الدخول ببيانات خاطئة 5-10 مرات متتالية بسرعة (في ثوانٍ قليلة)
3. بعد عدة محاولات

**النتيجة المتوقعة:**
- [ ] يجب أن يظهر رسالة: "تم تعليق محاولات تسجيل الدخول مؤقتاً بسبب محاولات متكررة"
- [ ] يجب أن يظهر عداد تنازلي للوقت المتبقي (مثل: "1:30" يعني دقيقة و30 ثانية)
- [ ] يجب أن يكون زر "تسجيل الدخول" معطلاً (disabled)
- [ ] يجب أن تظهر رسالة: "يمكنك المحاولة مرة أخرى بعد انتهاء العد التنازلي"
- [ ] بعد انتهاء الوقت، يجب أن تظهر رسالة: "يمكنك المحاولة الآن."

---

### ✅ د. اختبار 2FA (إذا كان مفعلاً)

#### 10. تسجيل الدخول بحساب مفعل 2FA → نموذج 2FA
**الخطوات:**
1. تأكد من تفعيل 2FA على حساب الاختبار (إذا كان متاحاً)
2. افتح `http://localhost:3000/login`
3. أدخل البريد وكلمة المرور لحساب مفعل 2FA
4. اضغط "تسجيل الدخول"

**النتيجة المتوقعة:**
- [ ] يجب أن يظهر نموذج 2FA بدلاً من نموذج تسجيل الدخول
- [ ] يجب أن يظهر عنوان: "التحقق بخطوتين"
- [ ] يجب أن يظهر حقل لإدخال رمز من 6 أرقام
- [ ] يجب أن تظهر رسالة: "أدخل الرمز المرسل إلى بريدك الإلكتروني"
- [ ] في بيئة التطوير، يجب أن يظهر رمز 2FA في Console (F12 → Console)

---

#### 11. إدخال رمز 2FA صحيح → تسجيل دخول
**الخطوات:**
1. من الاختبار السابق، بعد ظهور نموذج 2FA
2. افتح Console (F12 → Console) لرؤية رمز 2FA (في بيئة التطوير)
3. أدخل الرمز الصحيح في حقل 2FA
4. اضغط "تحقق"

**النتيجة المتوقعة:**
- [ ] يجب أن يتم تسجيل الدخول بنجاح
- [ ] يجب أن يتم حفظ token في localStorage/sessionStorage
- [ ] يجب أن يتم إعادة التوجيه إلى الصفحة الرئيسية

---

#### 12. إدخال رمز 2FA خاطئ → خطأ
**الخطوات:**
1. من الاختبار السابق، بعد ظهور نموذج 2FA
2. أدخل رمز خاطئ (مثل: `000000`)
3. اضغط "تحقق"

**النتيجة المتوقعة:**
- [ ] يجب أن يظهر خطأ: "رمز التحقق غير صحيح"
- [ ] يجب أن يهتز النموذج (shake animation)
- [ ] يجب أن يتم مسح حقل الرمز
- [ ] يجب أن يبقى نموذج 2FA مرئياً (لا يتم إعادة التوجيه)

---

## 📝 ملاحظات الاختبار

### في Console (F12 → Console):
- [ ] لا توجد أخطاء JavaScript
- [ ] رسائل التحذير واضحة ومفيدة
- [ ] Token يتم حفظه بشكل صحيح
- [ ] في حالة 2FA، يمكنك رؤية الرمز في Console (في بيئة التطوير)

### في Network (F12 → Network):
- [ ] طلبات API تعمل بشكل صحيح
- [ ] استجابة `/api/auth/login` تحتوي على `token` و `user` (في حالة النجاح)
- [ ] Status Code: 200 للنجاح، 401 للفشل، 403 للـ CAPTCHA، 429 للـ Rate Limit
- [ ] Cookies يتم تعيينها بشكل صحيح (httpOnly cookies)

### في Application/Storage (F12 → Application):
- [ ] Token محفوظ في localStorage/sessionStorage
- [ ] User data محفوظة بشكل صحيح
- [ ] لا توجد بيانات قديمة أو غير صحيحة

---

## ✅ النتيجة النهائية

بعد إكمال جميع الاختبارات، يجب أن:
- ✅ جميع اختبارات التحقق من الصحة تعمل بشكل صحيح
- ✅ تسجيل الدخول الناجح يعمل
- ✅ Token يتم حفظه بشكل صحيح
- ✅ معالجة الأخطاء تعمل بشكل صحيح
- ✅ CAPTCHA يظهر بعد 3 محاولات
- ✅ Rate limiting يعمل
- ✅ 2FA يعمل (إذا كان مفعلاً)

---

## 🔧 حل المشاكل

### إذا لم يعمل الخادم:
```bash
npm run dev
```

### إذا لم يوجد حساب تجريبي:
```bash
npm run create-test-account
```

### إذا لم يعمل CAPTCHA:
- تأكد من إعداد `HCAPTCHA_SITE_KEY` و `HCAPTCHA_SECRET` في `.env`

### إذا لم يعمل 2FA:
- تأكد من تفعيل 2FA على الحساب أولاً
- في بيئة التطوير، تحقق من Console لرؤية رمز 2FA


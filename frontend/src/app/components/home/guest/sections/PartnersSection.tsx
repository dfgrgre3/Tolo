'use client';

import { CONTAINER, TYPOGRAPHY, SECTION_HEADER, SECTION } from '../design-system';

/**
 * PartnersSection
 *
 * This section is intentionally hidden until real partner data
 * is available from the backend. Previously displayed fake partners
 * (AWS, Google, Microsoft, etc.) which was misleading.
 *
 * To re-enable: add a partners API endpoint to the backend,
 * fetch real data, and display it here.
 */
export function PartnersSection() {
  // No real partners data available yet — render nothing.
  return null;
}

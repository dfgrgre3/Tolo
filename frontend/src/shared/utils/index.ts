/**
 * Shared Utils Layer — الدوال المساعدة العامة
 *
 * @canonical `@/shared/utils`
 */

export {
  cn,
  formatDate,
  formatDateTime,
  formatRelativeTime,
  generateId,
  generateSecurePin,
  formatNumber,
  formatCurrency,
  formatPercentage,
  getRandomFloat,
  formatPrice,
  trimTrailingSlashes,
  toggleThemeWithTransition,
} from "@/lib/utils";

export { formatEGP } from "@/features/payments";

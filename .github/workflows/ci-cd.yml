name: Thanawy CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744
    
    - name: Setup Node.js
      uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.1.0
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Setup Go
      uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5.1.0
      with:
        go-version: '1.25'
        cache-dependency-path: backend/go.sum

    - name: Install Frontend Dependencies
      run: npm ci
    
    - name: Install Backend Dependencies
      working-directory: ./backend
      run: go mod download

    - name: Run Frontend Linter
      run: npm run lint
    
    - name: Run Frontend Type Check
      run: npm run type-check
    
    - name: Run Frontend Tests
      run: npm test

    - name: Run Backend Tests
      working-directory: ./backend
      run: go test -v ./...

    - name: Run Backend Linter
      uses: golangci/golangci-lint-action@55c2c1448f86e01eaae002a5a3a9624417608d84 # v6.1.1
      with:
        version: latest
        working-directory: backend

    - name: Security Audit (High and Critical)
      run: npm audit --audit-level=high
      # This will fail the build if high/critical vulnerabilities are found
    
    - name: Build Backend
      working-directory: ./backend
      run: go build -o main cmd/api/main.go

    - name: Build Frontend Docker Image
      run: docker build -t thanawy-app:${{ github.sha }} .
    
    - name: Build Backend Docker Image
      run: docker build -t thanawy-backend:${{ github.sha }} -f Dockerfile.backend .
    
    - name: Login to Docker Registry
      if: github.ref == 'refs/heads/main'
      uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.3.0
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Push Docker Images
      if: github.ref == 'refs/heads/main'
      run: |
        docker tag thanawy-app:${{ github.sha }} thanawy/thanawy-app:latest
        docker push thanawy/thanawy-app:latest
        docker tag thanawy-backend:${{ github.sha }} thanawy/thanawy-backend:latest
        docker push thanawy/thanawy-backend:latest

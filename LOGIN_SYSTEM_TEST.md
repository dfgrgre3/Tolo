# دليل اختبار نظام تسجيل الدخول

## ✅ التحقق من الاتصال بين الواجهة الأمامية والخلفية

### 1. اختبار نقطة نهاية API مباشرة

#### اختبار تسجيل الدخول الناجح:
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123!@#",
    "rememberMe": false
  }'
```

#### اختبار تسجيل الدخول ببيانات خاطئة:
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "wrong@example.com",
    "password": "WrongPassword123",
    "rememberMe": false
  }'
```

### 2. اختبار من الواجهة الأمامية

#### الخطوات:
1. افتح المتصفح على `http://localhost:3000/login`
2. تحقق من تحميل نموذج تسجيل الدخول بشكل صحيح
3. جرب تسجيل الدخول بحساب موجود

#### اختبارات الواجهة الأمامية:

**أ. اختبار التحقق من الصحة:**
- [ ] إدخال بريد إلكتروني غير صحيح → يجب أن يظهر خطأ
- [ ] إدخال كلمة مرور قصيرة (< 8 أحرف) → يجب أن يظهر خطأ
- [ ] ترك حقل فارغ → يجب أن يظهر خطأ

**ب. اختبار تسجيل الدخول الناجح:**
- [ ] إدخال بيانات صحيحة → يجب أن يتم تسجيل الدخول بنجاح
- [ ] يجب أن يتم حفظ الرمز المميز في localStorage/sessionStorage
- [ ] يجب أن يتم إعادة التوجيه إلى الصفحة الرئيسية

**ج. اختبار معالجة الأخطاء:**
- [ ] إدخال بيانات خاطئة → يجب أن يظهر رسالة خطأ واضحة
- [ ] محاولة تسجيل دخول متكررة → يجب أن يظهر CAPTCHA بعد 3 محاولات
- [ ] محاولة تسجيل دخول بعد rate limit → يجب أن يظهر رسالة انتظار

**د. اختبار 2FA (إذا كان مفعلاً):**
- [ ] تسجيل الدخول بحساب مفعل 2FA → يجب أن يظهر نموذج 2FA
- [ ] إدخال رمز 2FA صحيح → يجب أن يتم تسجيل الدخول
- [ ] إدخال رمز 2FA خاطئ → يجب أن يظهر خطأ

---

## 📚 ملفات الاختبار المتاحة

### 1. الاختبار التلقائي
```bash
# تشغيل الخادم أولاً (في نافذة terminal منفصلة)
npm run dev

# في نافذة terminal أخرى
npm run test:login
```

**الملفات:**
- `scripts/test-login-system.ts` - سكريبت الاختبار التلقائي
- `TEST_LOGIN_GUIDE.md` - دليل شامل للاختبارات

### 2. الاختبار اليدوي
راجع الملفات التالية للتعليمات التفصيلية:
- `MANUAL_TEST_STEPS.md` - خطوات الاختبار اليدوي التفصيلية
- `TEST_LOGIN_GUIDE.md` - دليل شامل للاختبارات

### 3. اختبار API مباشرة
```bash
# اختبار تسجيل الدخول
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "password": "Test123!@#", "rememberMe": false}'

# اختبار التسجيل
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email": "newuser@example.com", "password": "Test123!@#", "name": "Test User"}'
```

### 3. اختبارات الوحدة (Unit Tests)

#### اختبار API Client:
```typescript
// يجب اختبار:
- loginUser() مع بيانات صحيحة
- loginUser() مع بيانات خاطئة
- loginUser() مع 2FA
- verifyTwoFactor() مع رمز صحيح
- verifyTwoFactor() مع رمز خاطئ
```

#### اختبار Auth Service:
```typescript
// يجب اختبار:
- createTokens() مع بيانات صحيحة
- verifyToken() مع رمز صحيح
- verifyToken() مع رمز منتهي الصلاحية
- verifyToken() مع رمز غير صحيح
```

### 4. اختبارات التكامل

#### اختبار التدفق الكامل:
1. المستخدم يفتح صفحة تسجيل الدخول
2. المستخدم يدخل بيانات صحيحة
3. النظام يتحقق من البيانات في Backend
4. Backend يرسل الرمز المميز
5. Frontend يحفظ الرمز ويحدث الحالة
6. المستخدم يتم إعادة توجيهه إلى الصفحة الرئيسية

### 5. اختبارات الأمان

#### اختبارات الأمان المطلوبة:
- [ ] Rate limiting يعمل بشكل صحيح
- [ ] CAPTCHA يظهر بعد 3 محاولات فاشلة
- [ ] الرموز المميزة محمية بـ httpOnly cookies
- [ ] التحقق من صحة البيانات يعمل على الواجهة الأمامية والخلفية
- [ ] رسائل الخطأ لا تكشف معلومات حساسة

### 6. اختبارات الأداء

#### اختبارات الأداء:
- [ ] وقت استجابة API أقل من 2 ثانية
- [ ] لا توجد مشاكل في الذاكرة
- [ ] التحميل السريع للواجهة الأمامية

## 📋 قائمة التحقق الكاملة

### Backend API:
- [x] إصلاح مسار الاستيراد في route.ts
- [x] تحسين معالجة الأخطاء
- [x] إضافة التحقق من صحة الرموز المميزة
- [x] إضافة رؤوس الأمان
- [x] تحسين معالجة 2FA

### Frontend:
- [x] تحسين API Client
- [x] تحسين UserProvider
- [x] تحسين EnhancedLoginForm
- [x] تحسين معالجة الأخطاء
- [x] تحسين تجربة المستخدم

### إدارة الجلسات والرموز:
- [x] تحسين createTokens
- [x] تحسين verifyToken
- [x] إضافة التحقق من صحة البيانات

## 🚀 خطوات التشغيل

1. **تأكد من تشغيل الخادم:**
   ```bash
   npm run dev
   ```

2. **افتح المتصفح:**
   ```
   http://localhost:3000/login
   ```

3. **اختبر تسجيل الدخول:**
   - استخدم حساب موجود أو أنشئ حساب جديد
   - جرب سيناريوهات مختلفة (نجاح، فشل، 2FA، إلخ)

4. **راقب Console:**
   - افتح Developer Tools
   - تحقق من عدم وجود أخطاء
   - تحقق من طلبات API

5. **تحقق من Network Tab:**
   - تأكد من أن طلبات API تعمل بشكل صحيح
   - تحقق من رموز الحالة (200, 401, 403, إلخ)
   - تحقق من استجابات API

## 🔍 نقاط التحقق الإضافية

### في Console:
- لا توجد أخطاء JavaScript
- رسائل التحذير واضحة ومفيدة
- Token يتم حفظه بشكل صحيح

### في Network:
- طلبات API تعمل بشكل صحيح
- الاستجابات تحتوي على البيانات المطلوبة
- Cookies يتم تعيينها بشكل صحيح

### في Application/Storage:
- Token محفوظ في localStorage/sessionStorage
- User data محفوظة بشكل صحيح
- لا توجد بيانات قديمة أو غير صحيحة

## 📝 ملاحظات مهمة

1. **في بيئة التطوير:**
   - يمكنك رؤية رمز 2FA في Console
   - يمكنك استخدام الحساب التجريبي (test@example.com)

2. **اختبار Rate Limiting:**
   - حاول تسجيل الدخول 5 مرات ببيانات خاطئة
   - يجب أن يتم حظرك مؤقتاً

3. **اختبار CAPTCHA:**
   - بعد 3 محاولات فاشلة، يجب أن يظهر CAPTCHA
   - يجب إكمال CAPTCHA قبل المحاولة مرة أخرى

4. **اختبار 2FA:**
   - تأكد من تفعيل 2FA على حساب الاختبار
   - تحقق من استلام رمز 2FA (في Console في بيئة التطوير)

## ✅ النتيجة المتوقعة

بعد إكمال جميع الاختبارات، يجب أن يعمل نظام تسجيل الدخول بشكل:
- ✅ سلس وسريع
- ✅ آمن ومحمي
- ✅ يقدم رسائل خطأ واضحة
- ✅ يتعامل مع جميع الحالات بشكل صحيح
- ✅ متصل بشكل صحيح بين Frontend و Backend


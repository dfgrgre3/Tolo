# دليل تشخيص أخطاء تسجيل الدخول

## 🔍 خطوات التشخيص

### 1. التحقق من متغيرات البيئة (Environment Variables)

تأكد من وجود المتغيرات التالية في ملف `.env`:

```env
JWT_SECRET=your-secret-key-here (يجب أن يكون قوياً وطويلاً)
DATABASE_URL=your-database-url
NEXTAUTH_SECRET=your-secret-key (اختياري)
```

**ملاحظة مهمة**: يجب أن يكون `JWT_SECRET` قوياً وطويلاً (32 حرف على الأقل). إذا كان `your-secret-key`، سيتم رفض الطلب.

### 2. التحقق من اتصال قاعدة البيانات

تأكد من أن:
- قاعدة البيانات تعمل
- `DATABASE_URL` صحيح
- يمكن الوصول إلى قاعدة البيانات من الخادم

### 3. التحقق من سجلات الخادم (Server Logs)

افتح terminal وابحث عن:
- `Login error details:` - سيعرض تفاصيل الخطأ
- `Failed to create session:` - مشكلة في إنشاء الجلسة
- `Failed to generate tokens:` - مشكلة في إنشاء الرموز
- `Database error` - مشكلة في قاعدة البيانات

### 4. رموز الأخطاء الشائعة

| رمز الخطأ | المعنى | الحل |
|-----------|--------|------|
| `SERVER_ERROR` | خطأ داخلي في الخادم | راجع سجلات الخادم |
| `CONNECTION_ERROR` | مشكلة في الاتصال | تحقق من قاعدة البيانات |
| `INVALID_CREDENTIALS` | بيانات الدخول غير صحيحة | تحقق من البريد الإلكتروني وكلمة المرور |
| `VALIDATION_ERROR` | بيانات الطلب غير صحيحة | تحقق من تنسيق البيانات |
| `NOT_FOUND` | مسار API غير موجود | تحقق من مسار API |
| `RATE_LIMITED` | تم تجاوز الحد المسموح | انتظر قليلاً ثم حاول مرة أخرى |

## 🛠️ الإصلاحات المنفذة

### 1. تحسين معالجة الأخطاء
- ✅ إضافة معالجة أفضل لأخطاء الاتصال
- ✅ التحقق من JWT_SECRET قبل إنشاء الرموز
- ✅ التحقق من صحة الجلسات والرموز بعد إنشائها
- ✅ جعل تسجيل الأحداث الأمنية non-blocking

### 2. تحسين رسائل الخطأ
- ✅ رسائل خطأ واضحة بالعربية
- ✅ رموز أخطاء محددة لكل نوع من الأخطاء
- ✅ رسائل خطأ مختلفة حسب نوع المشكلة

### 3. تحسين استقرار النظام
- ✅ معالجة أفضل لأخطاء قاعدة البيانات
- ✅ عدم إيقاف تسجيل الدخول عند فشل العمليات غير الحرجة
- ✅ إعادة المحاولة التلقائية للأخطاء القابلة للاسترداد

## 📝 اختبار تسجيل الدخول

### اختبار من Terminal:

```bash
# اختبار تسجيل الدخول
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123!@#",
    "rememberMe": false
  }'
```

### اختبار من المتصفح:

1. افتح Developer Tools (F12)
2. اذهب إلى تبويب Console
3. اذهب إلى تبويب Network
4. حاول تسجيل الدخول
5. راجع الاستجابة في Network tab

## 🔧 حل المشاكل الشائعة

### المشكلة: `SERVER_ERROR` مع رسالة عامة

**الحل:**
1. راجع سجلات الخادم في terminal
2. تحقق من وجود `JWT_SECRET` في `.env`
3. تحقق من اتصال قاعدة البيانات
4. تأكد من أن جميع الجداول موجودة في قاعدة البيانات

### المشكلة: `CONNECTION_ERROR`

**الحل:**
1. تحقق من `DATABASE_URL` في `.env`
2. تأكد من أن قاعدة البيانات تعمل
3. تحقق من صلاحيات الاتصال

### المشكلة: `INVALID_RESPONSE_FORMAT`

**الحل:**
1. تحقق من أن الخادم يعيد JSON وليس HTML
2. راجع مسار API (`/api/auth/login`)
3. تأكد من أن الخادم يعمل بشكل صحيح

## 📞 الحصول على المساعدة

إذا استمرت المشكلة:
1. انسخ رسالة الخطأ الكاملة
2. انسخ سجلات الخادم من terminal
3. تحقق من متغيرات البيئة (بدون إظهار القيم الحساسة)
4. راجع ملف `LOGIN_SYSTEM_TEST.md` للاختبارات

